# Notices

## unDraw illustrations

The four empty-state illustrations in `resources/js/components/illustrations/`
are derived from open-source drawings by [unDraw](https://undraw.co), used under
the [unDraw licence](https://undraw.co/license). Each has been inlined as a Vue
SFC and recoloured by hand onto this project's palette; the geometry is unDraw's.

| File                   | Derived from                                     |
| ---------------------- | ------------------------------------------------ |
| `EmptyCart.vue`        | "Empty cart" (`empty-cart_574u`)                 |
| `NothingSaved.vue`     | "Shopping Favorites" (`shopping-favorites_spd0`) |
| `NothingToCompare.vue` | "Data Table" (`data-table_xmec`)                 |
| `NoOrders.vue`         | "Order Status" (`order-status_swsl`)             |

The licence permits commercial use and modification and requires no attribution;
this notice is recorded because the repository is public. It forbids
redistributing unDraw assets as a collection, so these four files exist only as
parts of this application and are not offered as an asset pack.
